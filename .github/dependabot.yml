version: 2
updates:
  # Python package: pyproject.toml only (uv.lock is deliberately untracked).
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"

  # Starlight docs site.
  - package-ecosystem: "npm"
    directory: "/website"
    schedule:
      interval: "weekly"
